{
  "name": "tic-tac-toe",
  "version": "1.0.0",
  "description": "tic-tac-toe in ES6",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebundle": "cp src/index.html dist/index.html",
    "bundle": "browserify src/game-start.js -t babelify --outfile dist/bundle.js"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-latest": "^6.24.1",
    "babelify": "^8.0.0",
    "browserify": "^14.5.0"
  }
}
